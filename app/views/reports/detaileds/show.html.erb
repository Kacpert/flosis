<div class="space-y-5">
  <div class="flex justify-between items-center">
    <h1 class="text-2xl font-bold tracking-tight" style="color: var(--color-on-surface)">Detailed Report</h1>
    <div class="flex gap-2">
      <%= link_to "CSV", export_csv_reports_detailed_path(request.query_parameters), class: "m3-btn m3-btn-text m3-btn-sm" %>
      <%= link_to "PDF", export_pdf_reports_detailed_path(request.query_parameters), class: "m3-btn m3-btn-text m3-btn-sm" %>
    </div>
  </div>

  <%# Filters %>
  <%= form_with url: reports_detailed_path, method: :get, class: "filter-panel" do |f| %>
    <%= render "shared/date_range_presets" %>
    <div class="flex flex-wrap gap-3 items-end">
      <div class="space-y-1">
        <label class="text-xs font-medium" style="color: var(--color-on-surface-variant)">From</label>
        <%= f.date_field :from, value: @from, class: "m3-text-field" %>
      </div>
      <div class="space-y-1">
        <label class="text-xs font-medium" style="color: var(--color-on-surface-variant)">To</label>
        <%= f.date_field :to, value: @to, class: "m3-text-field" %>
      </div>
      <div class="space-y-1">
        <label class="text-xs font-medium" style="color: var(--color-on-surface-variant)">Project</label>
        <%= f.select :project_id, @projects.map { |p| [p.name, p.id] },
            { include_blank: "All" }, class: "m3-text-field" %>
      </div>
      <div class="space-y-1">
        <label class="text-xs font-medium" style="color: var(--color-on-surface-variant)">Client</label>
        <%= f.select :client_id, @clients.map { |c| [c.name, c.id] },
            { include_blank: "All" }, class: "m3-text-field" %>
      </div>
      <div class="space-y-1">
        <label class="text-xs font-medium" style="color: var(--color-on-surface-variant)">User</label>
        <%= f.select :user_id, @users.map { |u| [u.name, u.id] },
            { include_blank: "All" }, class: "m3-text-field" %>
      </div>
      <div class="space-y-1">
        <label class="text-xs font-medium" style="color: var(--color-on-surface-variant)">Billable</label>
        <%= f.select :billable, [["Yes", "1"], ["No", "0"]],
            { include_blank: "All" }, class: "m3-text-field" %>
      </div>
      <%= f.submit "Apply", class: "m3-btn m3-btn-filled m3-btn-sm" %>
    </div>
  <% end %>

  <%# Stats %>
  <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.75rem;">
    <div class="stat-card">
      <div class="stat-label">Total</div>
      <div class="stat-number"><%= format_duration_hm(@total_seconds) %></div>
    </div>
    <div class="stat-card">
      <div class="stat-label">Billable</div>
      <div class="stat-number" style="color: var(--color-success)"><%= format_duration_hm(@billable_seconds) %></div>
    </div>
    <div class="stat-card">
      <div class="stat-label">Amount</div>
      <div class="stat-number">$<%= '%.2f' % @billable_amount %></div>
    </div>
  </div>

  <%# Entries table %>
  <div class="m3-card-outlined overflow-x-auto">
    <table class="m3-table">
      <thead>
        <tr>
          <th>Date</th>
          <th>Description</th>
          <th>Project</th>
          <th>Task</th>
          <th>User</th>
          <th>Tags</th>
          <th class="text-right">Duration</th>
          <th class="text-right">Amount</th>
        </tr>
      </thead>
      <tbody>
        <% @entries.each do |entry| %>
          <tr>
            <td class="text-sm" style="color: var(--color-on-surface-variant)"><%= entry.started_at.strftime("%b %-d") %></td>
            <td class="text-sm max-w-xs truncate" style="color: var(--color-on-surface)"><%= entry.description %></td>
            <td>
              <% if entry.project %>
                <span class="flex items-center gap-1.5">
                  <%= project_color_dot(entry.project.color) %>
                  <span class="text-sm"><%= entry.project.name %></span>
                </span>
              <% end %>
            </td>
            <td class="text-sm" style="color: var(--color-on-surface-variant)"><%= entry.task&.name %></td>
            <td class="text-sm" style="color: var(--color-on-surface-variant)"><%= entry.user.name %></td>
            <td>
              <div class="flex gap-1">
                <% entry.tags.each do |tag| %>
                  <span class="m3-tag-chip" style="color: <%= tag.color %>; border-color: <%= tag.color %>30; background-color: <%= tag.color %>10"><%= tag.name %></span>
                <% end %>
              </div>
            </td>
            <td class="text-right mono-duration text-sm"><%= entry.formatted_duration %></td>
            <td class="text-right text-sm" style="color: var(--color-on-surface-variant)">
              <% if entry.billable? %>$<%= '%.2f' % entry.billable_amount %><% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <% if @entries.empty? %>
    <div class="empty-state">
      <p class="empty-state-title">No entries for this period</p>
      <p class="empty-state-desc">Adjust your filters or date range</p>
    </div>
  <% end %>

  <% if @pagy.pages > 1 %>
    <div class="flex justify-center">
      <%== pagy_nav(@pagy) %>
    </div>
  <% end %>
</div>
